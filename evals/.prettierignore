# Generated by context/build-packs.mjs from dist/meta — minified on purpose,
# because the packs are what a model reads, not what a person reviews. Without
# this, `pnpm format` fails the moment anyone regenerates them.
context/packs/

# Model output and run logs. Reproducible, not reviewable.
candidates/
results/
