/**
 * Public surface of @ionbase/tokens.
 *
 * Everything here is generated from the IonBase Figma file — see
 * `src/figma/*.json` for the committed export and `scripts/` for the pipeline.
 * CSS custom properties ship separately at `@ionbase/tokens/css`.
 */
export * from './generated/index.js';
