{
  "summary": "A table of records with a toolbar, selection, and the three states everyone forgets.",
  "useWhen": [
    "showing a list of records the user filters, sorts, selects or acts on in bulk",
    "the row count is unknown at design time — which is what makes the empty and loading states mandatory rather than optional"
  ],
  "composes": [
    "Table",
    "TableHead",
    "TableBody",
    "TableRow",
    "TableCell",
    "Checkbox",
    "TableBatchBar",
    "Button",
    "Badge",
    "Alert",
    "SearchField",
    "MultiSelect",
    "Slider",
    "TagGroup",
    "SidePanel",
    "SidePanelLayout",
    "ContextMenu"
  ],
  "structure": [
    "A toolbar above the table: a SearchField (size sm, with an aria-label naming what it searches) and filter controls — a MultiSelect with `hideTags` for any filter that can match more than one value, and a range Slider (`defaultValue={[low, high]}`, with `formatOptions` for the unit) for a quantity bounded at both ends — duration, cost — which applies on `onChangeEnd`, not per step. They stay ordinary tab stops, not a Toolbar — see Toolbar's contract.",
    "Under the toolbar, once any filter is applied: a TagGroup of the active filters, each removable on its own — one tag per value of a multi-value filter, one for a range (\"Duration: 30–120 sec\") that resets both thumbs when removed — beside a Clear all Button. When the last one goes the row goes with it, so move focus to the SearchField.",
    "Between the filters and the table, a TableBatchBar, always rendered: hidden while nothing is selected, and once anything is, the count, \"Select all N\" while fewer than all matching rows are selected, the bulk actions and \"Clear selection\". Hold the selection in `useTableSelection({ total })` and clear it whenever the filters, the sort or the page size change — but not on a page change, so rows ticked on two pages can be acted on together.",
    "Table, with an `id` passed to the bar as `tableId`, and a TableHead whose first cell carries the select-all Checkbox — `headSelection(pageKeys)`, which reaches the page on screen only — and a TableBody whose rows each carry `rowSelection(key, label)` and `isSelected(key)`.",
    "Sortable columns: every header that sorts gets `sortDirection` — the current direction on the sorted column, `none` on the others — so each shows it can be sorted before anyone tries.",
    "A row's detail, when it is read beside the list rather than on its own page, goes in a SidePanel: Table and SidePanel inside a SidePanelLayout, opened from a per-row Button with `aria-expanded`, and closed when its row is filtered out. The table stays usable, so the next row can be opened without closing anything; below 768px it opens as a Drawer.",
    "A row's actions, when it has more than one, in a ⋯ MenuTrigger in its last cell, named for the row — \"Actions for Invoice reconciler\". For a table people work in all day, wrap each row in a ContextMenu given the same Menu element, so a right-click or Shift+F10 opens those actions too — a shortcut, never the only way to them.",
    "One region below the table for pagination or a load-more Button.",
    "The table region is replaced — not overlaid — by the empty, loading and error states."
  ],
  "drivenBy": {
    "hook": "useTableSort",
    "note": "It holds which column the rows are sorted by and which way, and `sortProps(column)` gives each sortable header TableCell its `sortDirection` and `onSort`. Choosing a new column sorts it in its `firstDirection` (ascending unless set — pass `descending` for dates and amounts); choosing it again reverses it. It does not reorder rows: sort them yourself from `sort`, or send it to the server when the table is paged. Only the sorted column gets `aria-sort`."
  },
  "states": {
    "loading": {
      "must": "Render the table's header row and a fixed number of skeleton rows, keeping the column widths.",
      "why": "swapping the whole region for a spinner collapses the layout and moves every control on the page when the data lands",
      "a11y": "put `aria-busy=\"true\"` on the container and announce the result count in a live region once it resolves"
    },
    "empty": {
      "must": "Say which of the two empties this is: no records exist yet, or the current filters match nothing. Offer the action that resolves it — create the first record, or clear the filters.",
      "why": "\"No results\" with no way forward is the single most common dead end in enterprise SaaS, and the two cases need opposite actions",
      "a11y": "the empty state carries the heading; do not leave the region unnamed"
    },
    "error": {
      "must": "Keep the toolbar usable, show an `Alert` with `intent=\"error\"` naming what failed, and offer a retry.",
      "why": "replacing the whole screen with an error throws away the user's filters and selection, so retrying means rebuilding the query by hand",
      "a11y": "Alert picks `role=\"alert\"` from the intent — do not add your own"
    },
    "partial": {
      "must": "When some rows load and others fail, show the rows you have and an Alert above them for the rest.",
      "why": "hiding successful rows because a sibling request failed is data loss the user cannot see"
    }
  },
  "propsUsed": {
    "Table": ["density", "isStriped"],
    "TableRow": ["selection", "isSelected"],
    "TableCell": ["scope", "align", "variant", "sortDirection", "onSort"],
    "Checkbox": ["isIndeterminate", "isDisabled"],
    "TableBatchBar": [
      "count",
      "total",
      "isAllMatching",
      "onSelectAll",
      "onClear",
      "tableId",
      "labels"
    ],
    "Button": ["variant", "size"],
    "Alert": ["intent", "title", "actions"],
    "TagGroup": ["label", "items", "onRemove"],
    "SearchField": ["size", "value", "onChange", "placeholder", "aria-label"],
    "MultiSelect": [
      "size",
      "aria-label",
      "options",
      "value",
      "onChange",
      "hideTags",
      "placeholder"
    ],
    "Slider": [
      "label",
      "value",
      "onChange",
      "onChangeEnd",
      "minValue",
      "maxValue",
      "step",
      "formatOptions"
    ],
    "SidePanel": [
      "isOpen",
      "onOpenChange",
      "title",
      "description",
      "footer",
      "size",
      "headingLevel"
    ],
    "ContextMenu": ["aria-label", "menu"]
  },
  "variantsUsed": {
    "Alert": {
      "intent": ["error"]
    },
    "Button": {
      "variant": ["primary-brand", "secondary", "destructive"]
    },
    "TableCell": {
      "align": ["trailing"]
    }
  },
  "a11y": {
    "requires": [
      "`aria-label` on Table — it names the scroll region, which is keyboard reachable",
      "`scope=\"col\"` on every header cell",
      "an `aria-label` on every row-selection Checkbox, and `isIndeterminate` on the select-all when the selection is partial",
      "a live region announcing the row count after a filter or search changes it — otherwise a screen-reader user has no idea the table changed",
      "a sortable header's label is its accessible name — the button inside the <th> reads the column name, and `aria-sort` on the sorted <th> says the direction"
    ],
    "notes": [
      "Bulk-action Buttons appearing on selection is a change of available actions, not just of styling. Announce it."
    ]
  },
  "antiPatterns": [
    {
      "dont": "a spinner replacing the whole table region",
      "do": "skeleton rows under the real header",
      "why": "the layout collapses and every control below the table jumps when data arrives"
    },
    {
      "dont": "one empty state for both \"nothing exists\" and \"nothing matches\"",
      "why": "the resolving action is opposite in each case — create something, or clear the filter"
    },
    {
      "dont": "a clickable <tr>",
      "do": "a Link or Button inside a cell",
      "why": "a row takes no focus, so a keyboard user can never fire it, and an interactive role on <tr> is not valid HTML"
    },
    {
      "dont": "destructive bulk actions with no confirmation",
      "do": "the DestructiveConfirm pattern",
      "why": "select-all plus delete is one click away from removing everything the filter matched"
    },
    {
      "dont": "wrapping Table in your own scroll container",
      "why": "Table already owns one and makes it keyboard reachable; a second is unreachable"
    },
    {
      "dont": "a sortable column with no indicator until it is clicked",
      "do": "`sortDirection=\"none\"` on every sortable header, so the neutral arrows show which columns sort",
      "why": "a header that sorts but looks like plain text is only found by accident"
    },
    {
      "dont": "sorting only the current page of a paged table",
      "why": "the reader sees \"A–Z\" and takes it as the whole list; send the sort to the server, which knows every row"
    },
    {
      "dont": "an Input with type=\"search\" and a magnifier icon as the table's search",
      "why": "it has no searchbox role, no Escape to clear and no labelled clear button — SearchField is the same box with that behaviour"
    },
    {
      "dont": "a multi-value filter as a MultiSelect showing its own tags above the active-filters row",
      "do": "`hideTags` on the MultiSelect, and one active-filter tag per value",
      "why": "the same values drawn twice, each removable, in two places that can disagree about focus"
    },
    {
      "dont": "a header checkbox that selects every matching row, on every page",
      "do": "`headSelection(pageKeys)` for the page, and TableBatchBar's \"Select all N\" for the rest",
      "why": "one tick reaching thousands of rows nobody has seen is how a bulk action destroys data by surprise"
    },
    {
      "dont": "sending an \"all matching\" selection to the server as a list of ids",
      "do": "the filters plus `selection.except` when `selection.mode` is `all`",
      "why": "the rows on pages never loaded have no ids on the client, and a list built from the pages that were is silently short"
    },
    {
      "dont": "refetching the table on a Slider's `onChange`",
      "do": "`onChange` to move the thumbs, `onChangeEnd` to apply the filter",
      "why": "`onChange` fires on every pixel of a drag: dozens of requests for one gesture, answered out of order, and a table that flickers through every one"
    },
    {
      "dont": "a Drawer for a row's detail beside the table",
      "do": "<SidePanelLayout><Table …/><SidePanel …/></SidePanelLayout>",
      "why": "a Drawer is modal: comparing two rows means closing and reopening, and a screen reader is told the table is gone"
    }
  ]
}
