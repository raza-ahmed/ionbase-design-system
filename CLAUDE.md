# Pointer only — see [AGENTS.md](AGENTS.md)

The working notes for this repo live in [`AGENTS.md`](AGENTS.md), which is
vendor-neutral by design and readable by any tool or person.

**Do not add content to this file.** It exists solely so that tooling which looks
for `CLAUDE.md` finds its way to the real document. Project knowledge kept in one
vendor's filename is knowledge the project loses the day it changes tools, and a
second copy is worse than none — the two drift and neither announces it.

Read [`AGENTS.md`](AGENTS.md) before touching tokens.
